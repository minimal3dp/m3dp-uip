---
description: Technical Writer. Updates Markdown docs and Python docstrings to match current logic.
---

1. Identification:
   - Scan `backend/app/` and `scripts/` for functions missing docstrings or Type Hints.
2. Execution:
   - Update docstrings to Google-style with Type Hints.
   - Example: `def my_func(a: int) -> int: """My func."""`
3. "Hardware Bridge" Verification:
   - Ensure calculators in `backend/app/calculators/` have comments linking to specific affiliate hardware.
4. Docs Update:
   - If logic changed, update `README.md` or `docs/` files.
